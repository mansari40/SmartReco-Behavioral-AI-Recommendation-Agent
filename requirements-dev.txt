# Local development / test extras. NOT installed on Render (production uses
# PostgreSQL + pgvector and never imports Chroma).
#
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# Vector DB — local Chroma fallback used only when DATABASE_URL is SQLite
# (local dev + the offline pytest suite).
chromadb==0.5.4
onnxruntime==1.20.1