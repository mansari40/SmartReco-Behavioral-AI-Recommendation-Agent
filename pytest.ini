[pytest]
asyncio_mode = auto
# Single session-scoped event loop so the async engine's pooled connections
# (asyncpg especially) are never handed to a different loop mid-suite.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning